{
  "private": true,
  "name": "website",
  "version": "0.1.2",
  "scripts": {
    "clean": "rm -rf .next out build",
    "build": "yarn clean && NODE_ENV=production next build && NODE_ENV=production next export",
    "dev": "next",
    "start": "next start"
  },
  "dependencies": {
    "@mapbox/rehype-prism": "^0.3.0",
    "isomorphic-unfetch": "^3.0.0",
    "next": "^7.0.2",
    "react": "^16.6.3",
    "react-dom": "^16.6.3",
    "react-live": "^1.12.0",
    "rsrc": "^0.0.2"
  },
  "devDependencies": {
    "@mdx-js/mdx": "^0.16.6",
    "@zeit/next-mdx": "^1.2.0"
  }
}

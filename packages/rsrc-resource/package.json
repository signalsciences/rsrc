{
  "name": "rsrc-resource",
  "author": "Signal Sciences",
  "description": "React resource library for declarative fetching",
  "version": "0.0.1",
  "publishConfig": {
    "access": "public"
  },
  "keywords": [
    "react",
    "resource",
    "fetch",
    "cache"
  ],
  "homepage": "https://github.com/signalsciences/rsrc",
  "bugs": {
    "url": "https://github.com/signalsciences/rsrc/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/signalsciences/rsrc.git"
  },
  "license": "MIT",
  "files": [
    "dist",
    "LICENSE",
    "README.md",
    "src"
  ],
  "main": "dist/cjs.js",
  "module": "dist/esm.js",
  "scripts": {
    "build": "yarn clean && rollup -c",
    "clean": "rm -rf dist",
    "dev": "yarn build --watch",
    "flow": "flow",
    "flow-typed": "npx flow-typed --packageDir ../../",
    "lint": "eslint src",
    "lint-staged": "lint-staged",
    "jest": "jest",
    "test": "yarn flow && yarn lint && yarn jest"
  },
  "lint-staged": {
    "src/**/*.js": [
      "eslint --fix",
      "git add"
    ]
  },
  "babel": {
    "presets": [
      "babel-preset-signalsciences"
    ]
  },
  "eslintConfig": {
    "extends": [
      "eslint-config-signalsciences"
    ]
  },
  "jest": {
    "preset": "jest-preset-signalsciences",
    "setupFiles": [
      "./setupJest.js"
    ]
  },
  "dependencies": {
    "path-to-regexp": "^2.4.0",
    "query-string": "^6.1.0",
    "rsrc-cache": "^0.0.1",
    "rsrc-fetch": "^0.0.1",
    "url-template": "^2.0.8"
  },
  "peerDependencies": {
    "react": ">=16.3"
  },
  "devDependencies": {
    "jest-fetch-mock": "^2.1.0"
  }
}
